# Session Notes: GitHub & Markdown

## Markdown Syntax
* **Fett**: `**Text**`
* *Kursiv*: `*Text*`
* Listen: `* Inhalt`

## GitHub Workflow
1. Branch erstellen: `git checkout -b branch-name`
2. Änderungen pushen: `git push`
3. Pull Request auf GitHub erstellen
