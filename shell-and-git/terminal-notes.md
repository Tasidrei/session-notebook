# How to create a new remote repository

1. Go to github.com and click the "+" icon -> "New repository".
2. Choose a repository name and set it to Public.
3. In Git Bash, link it using: git remote add origin <ssh-link>
4. Push your code using: git push -u origin main
