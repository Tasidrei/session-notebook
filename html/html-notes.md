we use insert any paragraph inside the <p></p> tags. It is used for normal content. It creates a bold of text with spacing above and below
we use <a> tag to create a hpyerlink to another page, section or a website. It is called link anchor.
<div></div> is called division or container as well as a box. One can group elements in that box to facilitate any work that needs to be done.
<img> is a tag used to display images. But you need a source (src) and a description for accessibility (alt)
<ul></ul> and <li></li> are unordered list that display bullet-points items
<ol></ol> are ordered lists and give you numbered lists 
<span> is an inline container, which is used to style or hightlight small parts of text without breaking the line
<header> <main> <footer> are semantic layout tags that give structure and meaning to the created page
<button></button> are used for actions